# Sprint 4: 管理 + 工具 + 偏好

## Task 1: 用户管理服务 (admin API)

**新建** `node-backend/src/services/adminService.ts` (~200 行)
- `getUsers(connectionId)` -- `SELECT User, Host, plugin, ... FROM mysql.user`
- `createUser(connectionId, req: CreateUserRequest)` -- `CREATE USER ... IDENTIFIED BY ...`
- `modifyUser(connectionId, username, req)` -- `ALTER USER ...`
- `dropUser(connectionId, username, host)` -- `DROP USER ...`
- `getUserPrivileges(connectionId, username, host)` -- `SHOW GRANTS FOR ...` 解析
- `modifyPrivileges(connectionId, username, grant[], revoke[])` -- `GRANT/REVOKE`

**新建** `node-backend/src/routes/admin.ts` (~120 行)
- `GET /api/admin/:connectionId/users`
- `POST /api/admin/:connectionId/users`
- `PUT /api/admin/:connectionId/users/:user` (user 格式: `username@host`)
- `DELETE /api/admin/:connectionId/users/:user`
- `GET /api/admin/:connectionId/users/:user/privileges`
- `PUT /api/admin/:connectionId/users/:user/privileges`

**修改** `node-backend/src/index.ts` -- 注册 admin 路由

## Task 2: 偏好设置服务 (preferences API)

**新建** `node-backend/src/services/preferencesService.ts` (~120 行)
- 存储到 `~/.reidisql/preferences.json`（复用 configStore 的模式）
- `getPreferences()` -- 返回 AppPreferences（含默认值合并）
- `updatePreferences(partial)` -- 深度合并更新
- 默认值定义（来自 shared/types.ts 中的 AppPreferences 结构）

**新建** `node-backend/src/routes/preferences.ts` (~40 行)
- `GET /api/preferences`
- `PUT /api/preferences`

**修改** `node-backend/src/index.ts` -- 注册 preferences 路由

## Task 3: 表工具服务 (tools API)

**新建** `node-backend/src/services/toolsService.ts` (~150 行)
- `maintenance(req: MaintenanceRequest)` -- 执行 CHECK/ANALYZE/CHECKSUM/OPTIMIZE/REPAIR TABLE
- `findText(req)` -- 在指定表的文本列中搜索关键词

```typescript
// maintenance 核心：
for (const table of tables) {
  const sql = `${operation.toUpperCase()} TABLE \`${table}\``;
  // 拼接 options: QUICK, FAST, MEDIUM, EXTENDED, CHANGED
  const [rows] = await pool.query(sql);
  // 解析 MySQL 返回的 status 行
}
```

**新建** `node-backend/src/routes/tools.ts` (~50 行)
- `POST /api/tools/maintenance`
- `POST /api/tools/find`

**修改** `node-backend/src/index.ts` -- 注册 tools 路由

## Task 4: 更新检查 API

**修改** `node-backend/src/routes/preferences.ts` 或 **新建** `node-backend/src/routes/update.ts` (~30 行)
- `GET /api/update/check` -- 检查 GitHub Releases 获取最新版本
- 返回 `{ currentVersion, latestVersion, hasUpdate, releaseUrl, releaseNotes }`
- 使用 `fetch()` 请求 GitHub API（无需鉴权）

## Task 5: 前端 API 扩展

**修改** `frontend/src/lib/api.ts` (+80 行)
- `adminApi`: listUsers / createUser / modifyUser / dropUser / getPrivileges / modifyPrivileges
- `preferencesApi`: get / update
- `toolsApi`: maintenance / findText
- `updateApi`: check

## Task 6: UserManager 前端组件

**新建** `frontend/src/components/UserManager.tsx` (~350 行)
- Antd Modal + Table 展示用户列表
- 列：用户名、主机、插件、锁定状态、密码过期、最大连接数
- 操作按钮：创建用户、编辑、删除、刷新
- 创建/编辑用户 Drawer：基本信息 Tab + 权限 Tab（Checkbox 组）+ 资源限制 Tab

## Task 7: Preferences 前端组件

**新建** `frontend/src/components/Preferences.tsx` (~300 行)
- Antd Modal + Tabs 分区：
  - General: 语言、主题（light/dark/system）、自动重连、更新检查
  - Editor: 字体、Tab 宽度、自动大写、代码补全延迟
  - Grid: 字体、最大列宽、最大行数、NULL 背景色
  - Logging: 最大行数、日志文件开关
- 读取并回写 preferences API

## Task 8: TableTools 前端组件

**新建** `frontend/src/components/TableTools.tsx` (~250 行)
- Antd Modal + Tabs:
  - Maintenance: 选择操作（CHECK/OPTIMIZE/REPAIR/ANALYZE）+ 表多选 + 执行 + 结果表格
  - Find Text: 输入搜索文本 + 表多选 + 匹配类型 + 搜索结果

## Task 9: SQLHelp + UpdateCheck 组件

**新建** `frontend/src/components/SQLHelp.tsx` (~100 行)
- 内置 MySQL 常用函数/关键字参考（静态数据）
- Antd Modal + Search + 分类列表

**新建** `frontend/src/components/UpdateCheck.tsx` (~80 行)
- Antd Modal 显示版本信息和更新内容
- 调用 updateApi.check

## Task 10: 集成到 App.tsx + DatabaseTree

**修改** `frontend/src/App.tsx`
- 导入 UserManager / Preferences / TableTools / SQLHelp / UpdateCheck
- Header 区域添加入口按钮：用户管理、偏好设置、SQL帮助、检查更新
- 右键菜单 / 工具栏入口：表工具（从 DatabaseTree 触发）

**修改** `frontend/src/components/DatabaseTree.tsx`
- 数据库节点右键菜单增加"表工具"选项

**修改** `node-backend/src/index.ts`
- 版本号更新为 `0.4.0-sprint4`

## Task 11: 验证

- 后端 `tsc --noEmit` 编译通过
- 前端 `tsc --noEmit` 编译通过
- E2E 测试：
  - 用户管理 CRUD（CREATE USER / SHOW GRANTS / DROP USER）
  - 偏好设置读写
  - 表维护操作（CHECK TABLE / OPTIMIZE TABLE）
  - 文本搜索
  - 更新检查
